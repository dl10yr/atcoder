[tool.poetry]
name = "python-jupyter"
version = "0.1.0"
description = ""
authors = ["dl10yr"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
jupyterlab = "^4.1.5"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
